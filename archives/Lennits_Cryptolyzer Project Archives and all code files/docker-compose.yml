
version: '3'
services:
  api:
    image: python:3.11
    command: bash -c "pip install fastapi uvicorn requests && uvicorn api.server:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"

  worker:
    image: python:3.11
    command: bash -c "pip install requests && python worker.py"
